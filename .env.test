# Biometric DID Integration Test Environment
# Task 4 Phase 1.5 - Standardized configuration for local + CI runs

# Environment classification (development|integration|staging|production)
ENVIRONMENT=integration

# API endpoints (used by test_api_auth.sh and demo wallet integration tests)
BASIC_API_URL=http://localhost:8000
SECURE_API_URL=http://localhost:8001
MOCK_API_URL=http://localhost:8002

# Authentication (secure server)
API_SECRET_KEY=test_api_key_admin_32_chars_long_abcdef123456
JWT_SECRET_KEY=jwt_secret_for_signing_tokens_32_chars_long
JWT_EXPIRATION_HOURS=1

# Feature flags (keep tests fast & predictable)
RATE_LIMIT_ENABLED=false
AUDIT_LOG_ENABLED=false
HTTPS_ONLY=false

# CORS settings for local dev servers
CORS_ORIGINS=http://localhost:3000,http://localhost:3003,http://localhost:5173

# Mock server tuning
MOCK_API_HOST=0.0.0.0
MOCK_API_PORT=8002
MOCK_API_RELOAD=false

# Cardano / Koios (public endpoint is read-only; override when self-hosting)
KOIOS_BASE_URL=https://api.koios.rest/api/v1
KOIOS_METADATA_LABEL=674
KOIOS_METADATA_BLOCK_LIMIT=1000
CARDANO_NETWORK=testnet  # Options: testnet, mainnet, preprod, preview

# Demo wallet / Jest integration
BIOMETRIC_API_URL=http://localhost:8002
BIOMETRIC_API_KEY=test_api_key_admin_32_chars_long_abcdef123456
RUN_API_TESTS=false  # Set to true when mock/secure servers are running

# Test harness defaults
API_KEY=test_api_key_admin_32_chars_long_abcdef123456
